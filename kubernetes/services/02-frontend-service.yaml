apiVersion: v1
kind: Service
metadata:
  namespace: vs-lab
  name: vs-frontend
  labels:
    service: vs-frontend
spec:
  ports:
    - name: "web"
      port: 3000
      targetPort: 3000
  selector:
    service: vs-frontend
    app: vs-lab
    tier: frontend