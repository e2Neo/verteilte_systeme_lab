apiVersion: v1
kind: Service
metadata:
  namespace: vs-lab
  name: vs-frontend
  labels:
    service: vs-frontend
spec:
  type: LoadBalancer
  ports:
    - name: "web"
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    service: vs-frontend
    app: vs-lab
    tier: frontend